## 2024-09-26 - [Disabled states and Keyboard Navigation]
**Learning:** Added visual feedback for disabled states (`disabled:opacity-50 disabled:cursor-not-allowed`) to action buttons controlled by JavaScript, addressing accessibility concerns regarding confusing inactive states. Added keyboard focus visibility to custom interactive elements (`focus-visible:ring-2 focus-visible:ring-blue-500`) and explicitly linked labels to form inputs (`for` attribute) which greatly improves accessibility for keyboard and screen-reader users without affecting mouse-driven interactions.
**Action:** Consistently ensure `for` attributes are used on form labels, explicitly declare `aria-label` for icon-only buttons, and use Tailwind's `focus-visible:` and `disabled:` modifier classes on custom buttons to ensure robust keyboard and screen-reader accessibility across all UI views.
